# Issues 4, 5, 6 Sequence and Verification Plan

## Order

Work order:

1. Issue `#4` - Backend: Duplicate detection & resolution endpoints
2. Issue `#5` - Frontend: Upload UI with file & folder selection
3. Issue `#6` - Frontend: Duplicate preview & rename/replace choice UI

Reason:

- `#6` depends on `#4`
- the remaining high-value work in `#5` also benefits from the backend contract added in `#4`
- closing the backend contract first reduces frontend rework

## Issue #4

### Phase 1: Backend contract and storage primitives

Goal:

- define duplicate preview DTOs
- define conflict policy contract: `reject`, `rename`, `replace`
- add storage primitives required for duplicate checks and trash moves

Verification:

- backend compiles
- OpenAPI generation includes preview endpoint and conflict policy fields

### Phase 2: Duplicate resolution engine

Goal:

- add duplicate preview endpoint
- implement rename strategy using `filename_(1).ext`
- implement replace strategy by moving existing objects to `trash/<original-path>` before write
- support single-file, folder, and multipart initiation flows

Verification:

- duplicate preview reports impacted paths
- rename writes deterministic suffixed names
- replace copies to `trash/<original-path>` and deletes the original before writing the new object

### Phase 3: Verification

Goal:

- add focused backend tests for preview, rename, replace, and multipart conflict handling
- regenerate backend OpenAPI artifacts

Verification:

- `cd backend && go test ./...`
- `make backend-openapi`

Status:

- complete locally on `2026-03-13`

Implemented in:

- `backend/internal/files/service.go`
- `backend/internal/files/http.go`
- `backend/internal/http/router.go`
- `backend/internal/storage/client.go`

## Issue #5

### Phase 1: Frontend contract sync and API generation pipeline

Goal:

- regenerate frontend client from backend OpenAPI
- ensure folder-upload codegen patch remains stable after backend contract changes

Verification:

- `cd frontend && npm run api:generate`
- `make frontend-lint`
- `make frontend-build`

Status:

- complete locally on `2026-03-13`

Notes:

- fixed `frontend/scripts/fix-folder-upload-client.mjs` so generated folder-upload client patches continue to work after the new backend contract changes

### Phase 2: Upload-panel migration to generated client

Goal:

- replace handwritten upload `fetch` calls with generated API client usage where practical
- keep destination-path behavior unchanged
- keep file and folder upload support intact
- keep multipart behavior intact
- improve error handling consistency

Verification:

- file upload still works into root and nested folders
- folder upload still preserves relative paths
- generated client remains the source of truth for request shapes

Status:

- pending

### Phase 3: Upload UX verification

Goal:

- verify upload progress and success/error states in the dashboard flow
- verify listing refresh after upload

Verification:

- `make frontend-lint`
- `make frontend-build`
- browser verification for file upload, folder upload, nested destination refresh

Status:

- pending

## Issue #6

### Phase 1: Frontend duplicate preview state

Goal:

- model duplicate preview requests and responses in the upload flow
- detect conflict responses and trigger preview flow

Verification:

- preview requests hit backend preview endpoint
- preview data is available to the UI

Status:

- pending

### Phase 2: Rename/replace choice UI

Goal:

- show impacted file list
- let user choose `rename` or `replace`
- wire selection back into upload and multipart initiation requests

Verification:

- duplicate preview dialog appears when conflicts exist
- rename choice uploads suffixed names
- replace choice moves originals to trash before upload

Status:

- pending

### Phase 3: End-to-end verification

Goal:

- verify file and folder conflict cases from the dashboard

Verification:

- `make frontend-lint`
- `make frontend-build`
- browser verification for file conflict rename/replace
- browser verification for folder conflict rename/replace

Status:

- pending

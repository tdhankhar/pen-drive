repos:
  - repo: local
    hooks:
      - id: frontend-lint
        name: frontend lint
        entry: npm run lint
        language: system
        pass_filenames: false
        files: ^frontend/
        stages:
          - pre-commit
